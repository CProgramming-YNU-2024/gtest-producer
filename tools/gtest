#!/usr/bin/env bash
# Tiny mock gtest binary that prints a version string
if [[ "$1" == "--version" || "$1" == "-v" ]]; then
  echo "gtest 1.2.3"
  exit 0
fi

# Default behaviour: print usage
cat <<EOF
mock gtest
Usage: $0 --version
EOF
exit 0
